#include <stdio.h>
#include <string.h>
#include <math.h>
#include "h.h"

int main()
{
    z = x + y; printf("X + Y = %d\n", z);
    z = x - y; printf("X - Y = %d\n", z);
    z = x * y; printf("X * Y = %d\n", z);
    z = x / y; printf("X / Y = %d\n", z);

age = 35; printf("Hello %s! Your gender is %s and you are %d years old!\n", name, sex, age );
int len = strlen(name); printf("The length of your name is %d\n", len);
int a = pow(6, 2); printf("The power of 'a' is equal to %d\n", a);
int b = 6; printf("The factorial of 'b' %d is equal to %d\n", b, f(6));
}