#include <stdio.h>
#include "h.h"
int main()
{
int num = 5;
    while (num > 0) {
    
  printf("%d\n", num);
  num--;
}
/*
    int table = 2; int max = 5;
    printf("Table\n");
    for(int i = 1; i <= table; i++)
    {
        for(int j = 0; j <= max; j++)
        {
            for(int k = 0; k <= max; k++)
            {
                printf("%d x %d x %d = %d\n", i, j, k, i * j * k);
            }
            printf("\n");
        }
    
    }

    for(i = 0; i <= 6; i++)
    {
        printf("%c\n", car[i]);
    }

    while(i <= 6)
    {
        printf("%c\n", model[i]);
        i++;
    }

    do
    {
        printf("%c\n", year[i]);
        i++;
    } 
    while (i <= 6);
*/
}